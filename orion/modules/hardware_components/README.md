This package's documentation is consolidated into `orion/README.md`.
See that file for instructions on attaching `ADS1115Driver` and the
`SerialGPSDriver` for the NEO-7M GPS.
This folder contains small interfaces and example driver stubs to make it easy
